namespace Utilidades {
    export function saludo(nombre: string): string{
        return `Hola ${nombre}`
    }

    export function despedida(nombre: string): string{
        return `Adiós ${nombre}`
    }
}